# ConstraintOS Schema Reference

This manual will document every registered schema, its purpose, primary object, validation rules, and relationships. It will be generated from the schema registry wherever practical to keep documentation synchronized with implementation.